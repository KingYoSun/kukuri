# v0.2.5-preview.1 リリース・Community Node更新

## 現在の状態

- 全5種クライアントとCommunity Node4イメージを同一sourceから公開し、既存GCP VMへ反映する。
- あわせて本番の一般 moderation provider を self-host VLM（`openai-compatible-vlm`）から専用の `openai-moderation`（`omni-moderation-latest`）へ切り替える。手順は [OpenAI Moderationの運用](../runbooks/community-node-openai-moderation.md) に従う。
- 受入条件: 同一sourceのWindows NSIS、Linux AppImage／Deb、CLI x86_64／aarch64の5本体、3 updater entryの実署名・改変拒否、公開後取得照合。CN4 imageのdigest／revision照合、backup後の非置換更新、`--force-probe` を含むreadiness・公開境界の確認。
- #1051 系（C2〜C4）の統合確認は [#1068](https://github.com/kukuri-app/kukuri/issues/1068) で行う。

## sourceと検証

- tag: `v0.2.5-preview.1`。
- 版同期は区分Aの機械的保守。workspace・Tauri・frontendを0.2.5へ同期。Cargo.lockは自前29 packagesのversionだけが変わり、外部依存と依存関係は不変。
- `cargo xtask release-check v0.2.5-preview.1`、`git diff --check` 成功。
