# v0.2.8-preview.1 リリース

## 現在の状態

- クライアント5本体（Windows NSIS、Linux AppImage／Deb、CLI x86_64／aarch64）を公開する準備中。Community Node の image 更新・VM 反映は含めない。
- 目的は v0.2.7-preview.1 以降の main の変更（URL パースと OGP プレビュー #1179、カスタムリアクション名の tooltip #1178、composer への画像の貼り付け #1177、画像 viewer のクリッピング修正 #1175 など）を配布し、あわせて Namespace へ移した release workflow（[#1180](https://github.com/kukuri-app/kukuri/issues/1180)）を実際の release で確認すること。実 release での確認はユーザー承認済み（2026-09-19）。
- tag `v0.2.8-preview.1`（source `e720649d`）の公開 run 35415877964 は linux-verify が Namespace の Ubuntu 22.04 image に PowerShell が無いことで失敗し、Release は作成されていない（詳細は [#1180 の作業記録](./2026-09-19-1180-release-namespace.md)）。tag は上書きせず、修正後に同じ 0.2.8 を `v0.2.8-preview.2` として公開する。
- 受入条件は v0.2.7-preview.1 と同じ: 同一sourceの5本体、3 updater entryの実署名・改変拒否、公開後取得照合。

## sourceと検証

- 版同期は区分Aの機械的保守。workspace・Tauri・frontendを0.2.8へ同期。Cargo.lockは自前packagesのversionだけが変わり、外部依存と依存関係は不変。
- `cargo xtask release-check v0.2.8-preview.1`、`git diff --check` 成功。
