# 2026-03-29 desktop channel-first sidebar and epoch lifecycle

- PR: local workspace change for channel-first sidebar, unified channel actions, and epoch handoff lifecycle. PR identifier is pending.
- Figma: [desktop shell IA v2 context header and composer compacting](https://www.figma.com/design/36DZOCnFS8mq5GRMp3WJc7?node-id=15-2). `Review/DesktopShellWorkspaceGallery/WorkspacePatternsGallery` を `iframe.html?id=review-desktopshellworkspacegallery--workspace-patterns-gallery&viewMode=story` から capture し、left rail の `CHANNEL` section、topic child channel accordion、`Timeline / Live / Game / Profile` の updated shell patterns を同 file に追加した。2026-03-29 の follow-up で review sidebar card の `CHANNEL` は default-closed accordion 表現へ更新し、`Tracked Topics` が clip されない状態に揃えた。さらに active topic の child selection には `Public` row と `Channels` group を明示し、left rail だけで現在の scope が判別できる表現へ差し替えた。
- Preview: PR-visible preview should use `Review/DesktopShellWorkspaceGallery/WorkspacePatternsGallery`.
- Storybook review surface: `Review/DesktopShellWorkspaceGallery/WorkspacePatternsGallery`, `Shell/ShellFrame/Default`, `Extended/PrivateChannelPanel/Ready`
- Summary: `channel` を `topic` 配下の selection として再定義し、`Channels` tab と `#/channels` route を廃止した。left rail は `ADD TOPIC -> CHANNEL -> Tracked Topics` に再編し、active topic の子要素として joined channel を表示する single-expand accordion に変えた。channel action は `Join` と `Share` に統一し、`Join Invite / Join Grant / Join Share` と `Create Invite / Create Grant / Create Share`、manual `Freeze / Rotate` を primary UI から外した。runtime/domain では `invite_only` を含む全 audience を epoch-aware に揃え、owner write/share 前の auto rotate と participant への epoch handoff auto distribution / auto apply を正本にした。
- User flow summary: user は left rail の topic 行で public scope、子 channel 行で private scope を選ぶ。選択結果は `Timeline / Live / Game` の list/create scope にそのまま反映される。channel 作成、token 貼り付け、`Join`、selected channel 向け `Share` は 모두 left rail で完結し、workspace switch は `Timeline / Live / Game / Profile` のみになる。existing participant は new epoch や refreshed invite を background redeem で自動適用し、manual re-join を要求されない。
- Review result: channel-first IA と unified epoch lifecycle の組み合わせを採用。scope の locus of control を left rail へ寄せ、workspace tab は content mode だけに責務を限定した。private audience の operational complexity は runtime へ押し込み、user-facing control 数を減らす方向を維持する。
- Shneiderman: consistency は left rail selection を `Timeline / Live / Game` で共通の scope source にすることで維持した。shortcuts は `Join` / `Share` の統合と channel child accordion による click 数削減で改善した。informative feedback は selected topic/channel state、audience badge、share token notice で維持した。dialog closure は create/join/share 後に selected channel と route が即座に更新される形で担保した。error prevention は legacy `#/channels` normalization と manual epoch control の撤去で補強した。internal locus of control は hidden scope dropdown をやめて left rail selection を正本にすることで強化した。short-term memory load は `channel is a child of topic` を layout へ埋め込むことで下げた。
- Exceptions: Figma capture は WSL 内 Linux Chrome からではなく Windows 側 browser 起動で完了した。capture script は `.storybook/preview-head.html` に既存で入っていたため、この slice では追加・削除していない。manual smoke は未実施。
- Validation: `cd apps/desktop && npx pnpm@10.16.1 exec tsc --noEmit`, `cd apps/desktop && npx pnpm@10.16.1 test`, `cd apps/desktop && npx pnpm@10.16.1 exec playwright test tests/playwright/extended-flow.spec.ts tests/playwright/hash-routing.spec.ts`, `cargo xtask desktop-ui-check`, `cargo check -p kukuri-app-api`, `cargo check -p kukuri-desktop-runtime`, `cargo check`, `cargo xtask check`, `cargo xtask test` は local pass。
