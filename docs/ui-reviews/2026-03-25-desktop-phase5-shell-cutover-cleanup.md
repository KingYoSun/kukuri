# 2026-03-25 desktop phase5 shell cutover cleanup

- PR: local workspace change for shell UI production migration Phase 5. PR identifier is pending.
- Figma: なし。Phase 5 は router/store cutover と cleanup slice として扱い、Phase 1-4 で承認済みの shell surface を production path に固定する作業を主対象にした。新規 standalone Figma proposal は作成していない。例外として受け入れた。
- Preview: PR-visible preview should use `Shell/ShellTopBar/Default`, `Shell/ShellNavRail/Default`, `Shell/ContextPane/Default`, `Shell/SettingsDrawer/DiscoveryOpen`, `Core/TopicNavList/Default`, `Core/TimelineFeed/Default`, `Core/ThreadPanel/Default`, `Core/AuthorDetailCard/Selected`, `Core/PostMedia/VideoPlayable`.
- Storybook review surface: `Shell/ShellFrame/Default`, `Shell/ShellTopBar/Default`, `Shell/ShellNavRail/Default`, `Shell/ContextPane/Default`, `Shell/SettingsDrawer/DiscoveryOpen`, `Core/CoreProductFlow/WideWorkspace`, `Core/TopicNavList/Default`, `Core/TimelineFeed/Default`, `Core/ThreadPanel/Default`, `Core/AuthorDetailCard/Selected`, `Core/PostMedia/VideoPlayable`
- Summary: Phase 5 では shell UI を唯一の本番 path に固定し、`App({ api })` の外部 surface を維持したまま内部へ `HashRouter + zustand` を導入した。primary route は `#/timeline`, `#/channels`, `#/live`, `#/game`, `#/profile` に固定し、search param は `topic`, `timelineScope`, `composeTarget`, `context`, `threadId`, `authorPubkey`, `settings` に限定した。`legacy-shell.css` は削除し、旧 shell class 群は `.shell-phase1` namespaced stylesheet に移した。backend、Tauri invoke、`DesktopApi`、domain contract は変更していない。
- User flow summary: navigation state は hidden persistence ではなく hash route を唯一の source とし、thread / author / settings の deep-link と back/forward を維持した。invalid route param は crash ではなく active topic の public timeline へ fallback し、malformed `authorPubkey` と空 thread route は context close + URL normalize に倒した。required direct stories を追加し、`#/profile`, `#/channels`, settings deep-link, history, narrow-width route/no-overflow を Playwright で押さえた。
- Review result: shell-phase1 path が production の唯一 path になり、legacy shell stylesheet 依存は削除済み。Storybook preview は router decorator と `.shell-phase1` wrapper を持ち、Phase 4 の global selector collision 前提 workaround に依存せず direct surface を確認できる。frontend gate として `cargo xtask desktop-ui-check` を追加し、Linux CI lane に Playwright Chromium install を入れた。human review / PR attach は pending。
- Shneiderman: 一貫性は primary section path、settings/context param、shared shell primitive の vocabulary を固定することで担保した。informative feedback は hash 正規化、active nav / current settings section、inline error のまま維持した。error prevention は invalid route fallback と `replace` normalization で補った。internal locus of control は back/forward で primary section と context/settings を戻せる形に整理した。short-term memory load は direct deep-link と settings/context persistence を route に集約することで下げた。
- Exceptions: `App.tsx` の domain orchestration はこの slice でも 1 file 内に残しており、zustand slice module の物理分割までは行っていない。Phase 5 は cleanup slice のため、新規 standalone Figma proposal は例外として省略した。
- Validation: `cargo xtask desktop-ui-check`, `cargo xtask check`, `cargo xtask e2e-smoke` は local pass。`cargo xtask test` は `kukuri_docs_sync::tests::public_replica_syncs_over_custom_relay_seed_peers` が 1 回 timeout で flaky failure を返したが、直後の `cargo test -p kukuri-docs-sync public_replica_syncs_over_custom_relay_seed_peers -- --nocapture` rerun は pass。frontend 単体でも `cd apps/desktop && npx pnpm@10.16.1 typecheck`, `test -- src/App.test.tsx`, `storybook:build`, `test:e2e:browser` は local pass。manual smoke (`tauri:dev` on Linux, packaged Windows app) は未実施。
