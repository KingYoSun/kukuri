# 2026年03月09日 完了タスク

最終更新日: 2026年03月09日

## Community Node 認証時の relay URL 汚染と iroh path warning を修正

- [x] `wss://api.kukuri.app/relay` fallback 混入を解消した。
  - `kukuri-tauri/src-tauri/src/presentation/handlers/community_node_handler.rs` に multi-node / bootstrap node 向けの再現 test を追加し、`base_url + /relay` fallback が不正に混入する条件を固定した。
  - bootstrap descriptor の `http` endpoint が config `base_url` と一致しない場合でも、`roles.bootstrap` の `endpoints.ws` を relay URL 解決に利用するよう修正した。
  - `cargo test resolve_nostr_relay_urls --lib -- --nocapture` を Docker 実行し、7 tests PASS を確認した。
- [x] `MaxPathIdReached` / `PATH_ABANDON` の原因になっていた relay-only lookup 汚染を抑止した。
  - `kukuri-tauri/src-tauri/src/infrastructure/p2p/utils.rs` に `sanitize_remote_endpoint_addr_with_preferred_relays()` を追加し、configured custom relay がある場合は remote endpoint の relay URLs を custom relay のみに正規化するよう修正した。
  - `kukuri-tauri/src-tauri/src/infrastructure/p2p/iroh_network_service.rs` と `iroh_gossip_service.rs` の peer 登録経路へ同正規化を適用した。
  - `relay_only_add_peer_prefers_configured_custom_relay_in_lookup` を含む connectivity tests を Docker 実行し PASS を確認した。
- [x] live-path でも再発しないことを確認した。
  - `E2E_SPEC_PATTERN=./tests/e2e/specs/community-node.end-to-end.spec.ts ./scripts/test-docker.ps1 e2e-community-node` を実行し PASS。
  - `tmp/logs/community-node-e2e/20260309-001451.log` を検索し、`api.kukuri.app/relay` / `404 Not Found` / `MaxPathIdReached` / `PATH_ABANDON` は不在であることを確認した。

## 検証

- [x] `./scripts/test-docker.ps1 rust`: pass
- [x] `cargo test resolve_nostr_relay_urls --lib -- --nocapture` 相当の Docker 実行: pass
- [x] `cargo test relay_only_add_peer --lib -- --nocapture` 相当の Docker 実行: pass
- [x] `E2E_SPEC_PATTERN=./tests/e2e/specs/community-node.end-to-end.spec.ts ./scripts/test-docker.ps1 e2e-community-node`: pass
- [x] `gh act --workflows .github/workflows/test.yml --job format-check`: pass
- [x] `gh act --workflows .github/workflows/test.yml --job native-test-linux`: pass
- [x] `gh act --workflows .github/workflows/test.yml --job community-node-tests`: pass

## Linux クライアントの account / Community Node 設定永続化を修正

- [x] Linux headless / keyring unavailable 環境でも account と Community Node 認証状態が再起動後に復元されるようにした。
  - `kukuri-tauri/src-tauri/src/infrastructure/storage/secure_storage.rs` の fallback を file-backed に変更し、`dirs::data_local_dir()/kukuri/secure_storage_fallback.json` へ永続化するようにした。
  - Rust test `fallback_persistence_restores_current_account_after_in_memory_reset` で file-backed fallback の復元を固定した。
- [x] live-path でも再起動後に onboarding へ戻らないことを確認した。
  - `kukuri-tauri/tests/e2e/specs/community-node.persistence.spec.ts` を追加し、account 作成 -> Community Node 認証 -> `browser.reloadSession()` -> home / settings / token status 復元を E2E で検証した。
  - `E2E_SPEC_PATTERN=./tests/e2e/specs/community-node.persistence.spec.ts ./scripts/test-docker.ps1 e2e-community-node` を実行し PASS。

## Community Node 設定保存・認証・role 変更時の false failure toast を修正

- [x] 設定保存・認証・role 変更後に false failure toast が出る不整合を解消した。
  - `CommunityNodePanel` の unit reproducer を追加し、成功した認証後の trust provider refresh 失敗が user-facing error になっていた条件を固定した。
  - `trust provider` / `pending join requests` の背景 query 失敗は `showToast: false` でログのみ扱うよう変更し、`CommunityNodePanel.test.tsx` 10 tests PASS を確認した。
- [x] live-path E2E で false failure toast が再発しないことを確認した。
  - `community-node.settings.spec.ts` で config save / authenticate / role change を実経路で再現し、failure toast が出ず UI 状態と backend state が一致することを確認した。
  - `./scripts/test-docker.ps1 e2e-community-node`（`E2E_SPEC_PATTERN=./tests/e2e/specs/community-node.settings.spec.ts`）で PASS を確認した。
- [x] タスク管理を更新した。
  - `docs/01_project/activeContext/tasks/status/in_progress.md` から当該タスクを削除した。
  - `docs/01_project/progressReports/2026-03-09_community_node_settings_toast_fix.md` を追加した。

## 追加検証

- [x] `E2E_SPEC_PATTERN=./tests/e2e/specs/community-node.persistence.spec.ts ./scripts/test-docker.ps1 e2e-community-node`: pass
- [x] `E2E_SPEC_PATTERN=./tests/e2e/specs/community-node.settings.spec.ts ./scripts/test-docker.ps1 e2e-community-node`: pass
