# 2025年11月06日 完了タスク

最終更新日: 2025年11月06日

## GitHub Actions Test ワークフロー修正

- [x] `Native Test (Linux)` ジョブが `Run TypeScript tests` ステップで失敗していた問題を解消し、ワークフローの再実行準備を完了
  - 2025年11月06日: `gh run view 19131212382 --job 54672164051 --log` で Vitest 実行時に `@tauri-apps/api/event.listen` 呼び出しが `transformCallback` 未定義で落ちていることを確認。
  - 2025年11月06日: `useDirectMessageEvents` に Tauri 実行環境の存在チェックを追加し、jsdom 環境ではイベント購読をスキップするよう改修。`useSyncManager` テストのモックを `refreshCacheMetadata` まで含めるよう調整。
  - 2025年11月06日: `pnpm vitest run` と `gh act -W .github/workflows/test.yml -j native-test-linux` を実行し、TypeScript テストとジョブ全体がローカルで成功することを確認。
