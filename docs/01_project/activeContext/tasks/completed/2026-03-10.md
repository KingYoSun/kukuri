# 2026年03月10日 完了タスク

最終更新日: 2026年03月10日

## 公開 Community Node 配下のタイムライン同期 / 投稿伝播失敗を修正

- [x] 公開 Community Node 設定でも loopback relay を採用しないよう修正した。
  - `kukuri-tauri/src-tauri/src/presentation/handlers/community_node_handler.rs` で、公開 `base_url` に対して descriptor が `ws://localhost:8082/relay` などの loopback / unspecified relay URL を返した場合は破棄し、`https://api.kukuri.app` のような公開 URL から導出される `wss://.../relay` fallback を使うよう修正した。
  - `resolve_nostr_relay_urls_ignores_loopback_descriptor_ws_for_public_base_url` と `resolve_nostr_relay_urls_keeps_loopback_descriptor_ws_for_loopback_base_url` を追加し、公開 node とローカル node の両方の挙動を固定した。
- [x] 投稿同期で UUID を Nostr event ID として扱っていた不整合を修正した。
  - `kukuri-tauri/src-tauri/src/infrastructure/database/sqlite_repository/queries.rs` の `SELECT_UNSYNC_EVENTS` を更新し、64 桁 hex の `event_id` だけを未同期イベント対象に絞った。
  - `kukuri-tauri/src-tauri/src/infrastructure/database/sqlite_repository/events.rs` に `get_unsync_events_skips_local_uuid_posts` を追加し、ローカル post UUID が `Invalid event ID format` を起こさないことを確認した。
- [x] 同期失敗後に再試行できなくなる状態固着を修正した。
  - `kukuri-tauri/src-tauri/src/application/services/sync_service.rs` の `start_sync()` で、失敗時でも `is_syncing` を必ず `false` へ戻すよう修正した。
  - `start_sync_resets_is_syncing_after_failure` を追加し、1 回目の失敗後でも 2 回目の同期を開始できることを確認した。
- [x] 起動直後同期の `EventManager not initialized` を修正した。
  - `kukuri-tauri/src-tauri/src/state.rs` で、active account がある場合は startup sync 前に `EventManager` を keypair で初期化するよう変更した。
  - 初期化失敗時は app 起動を止めず `warn` のみ残す挙動にした。
- [x] タスク管理を更新した。
  - `docs/01_project/progressReports/2026-03-10_community_node_timeline_sync_fix.md` を追加した。
  - `docs/01_project/activeContext/tasks/status/in_progress.md` は、完了したコード修正を未完了一覧から外し、残る live-path 確認だけを未解決項目として記録した。

## Community Node の profile update なし実機相当 E2E を追加

- [x] 投稿時に保存済みローカル profile metadata を topic へ自動伝播する実装を追加した。
  - `kukuri-tauri/src-tauri/src/infrastructure/event/manager/publishing.rs` で、topic post publish 時に現在ユーザーの保存済みプロフィール metadata を同一 topic へ best-effort で先行 broadcast / relay publish するよう修正した。
  - `kukuri-tauri/src-tauri/src/application/shared/tests/event/manager_tests.rs` と `kukuri-tauri/src-tauri/src/application/shared/tests/event/mocks.rs` に、保存済み profile metadata が post 前に送出されることを確認する Rust test を追加した。
- [x] profile update を伴わない 2-client 実機相当 E2E を追加した。
  - `kukuri-tauri/tests/e2e/specs/community-node.profile-resolution.spec.ts` で、publisher / listener がそれぞれ自分自身の profile をローカル保持済みの状態から通常投稿を行い、listener 側 timeline/thread の display name / avatar が自動解決されることを直接 assertion するようにした。
  - `kukuri-tauri/tests/e2e/helpers/peerHarness.ts` と `kukuri-tauri/src/components/posts/PostCard.tsx` を調整し、avatar URL を E2E / unit test から安定して検証できるようにした。
- [x] フロント側の profile 解決検証を補強した。
  - `kukuri-tauri/src/lib/profile/authorProfileResolver.ts` と `kukuri-tauri/src/lib/profile/authorProfileResolverCache.ts` を追加し、永続 profile の再取得と test 間キャッシュリセットを分離した。
  - `kukuri-tauri/src/tests/setup.ts`、`kukuri-tauri/src/tests/unit/components/posts/PostCard.test.tsx`、`kukuri-tauri/src/tests/unit/lib/profile/authorProfileResolver.test.ts` を更新し、native test 環境でも profile / avatar の assertion が崩れないようにした。

## 検証

- [x] `./scripts/test-docker.ps1 rust`: pass
- [x] `./scripts/test-docker.ps1 ts`: pass
- [x] `./scripts/test-docker.ps1 e2e-community-node`: pass
- [x] `gh act --workflows .github/workflows/test.yml --job format-check`: pass
- [x] `gh act --workflows .github/workflows/test.yml --job native-test-linux`: pass
- [x] `gh act --workflows .github/workflows/test.yml --job community-node-tests`: pass
