## kukuri-tauri React/関連ライブラリ最新化

- [x] `react` / `react-dom` を `19.2.3`、`@types/react` / `@types/react-dom` を `19.2.x` へ更新。
- [x] React 周辺（`@vitejs/plugin-react` / `eslint-plugin-react-hooks` / `@testing-library/react` / TanStack 系）を最新化。
- [x] 未使用依存の整理: `react-day-picker` / `@testing-library/react-hooks` を削除。
- [x] `pnpm-workspace.yaml` の `onlyBuiltDependencies` に `esbuild` を追加し、Docker 環境でもビルドスクリプトが実行されるように調整。
- [x] React 19 の型差分に伴う TypeScript エラーを修正（`MarkdownPreview` の props 型付け）。
- [x] `MainLayout` のユニットテストを React 19 で安定させるため、レイアウト子コンポーネントをモック化しつつ動的 import に変更。
- [x] `eslint-plugin-react-hooks` v7 の新ルールが既存コードに対して過剰に厳しいため、該当ルールを無効化して既存運用と整合させた。

### 実行コマンド

- `./scripts/test-docker.ps1 all`
- `gh act --bind --workflows .github/workflows/test.yml --job format-check --container-options "--user 0" -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:full-latest --env NPM_CONFIG_PREFIX=/tmp/npm-global`（ログ: `tmp/logs/gh_act_format-check_20251218-115610.log`）
- `gh act --bind --workflows .github/workflows/test.yml --job native-test-linux --container-options "--user 0" -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:full-latest --env NPM_CONFIG_PREFIX=/tmp/npm-global --env CARGO_TEST_THREADS=1 --env RUST_TEST_THREADS=1`（ログ: `tmp/logs/gh_act_native-test-linux_20251218-121243.log`）

## kukuri-tauri 依存関係更新（lucide-react/react-markdown/uuid）

- [x] `lucide-react` を `^0.561.0`、`react-markdown` を `10.1.0`、`uuid` を `^13.0.0` へ更新。
- [x] `react-markdown` v10 の `className` props 廃止に追従し、`MarkdownPreview` を wrapper `div` 適用に変更。

### 実行コマンド

- `./scripts/test-docker.ps1 ts`
- `./scripts/test-docker.ps1 lint`
- `gh act --workflows .github/workflows/test.yml --job format-check --env NPM_CONFIG_PREFIX=/tmp/npm-global`
- `gh act --workflows .github/workflows/test.yml --job native-test-linux --env NPM_CONFIG_PREFIX=/tmp/npm-global`

## kukuri-tauri Vite/Vitest 最新化

- [x] `vite` を `^7.3.0`、`vitest` を `^4.0.16` へ更新。
- [x] 周辺更新: `@vitest/ui` / `@vitest/utils` / `@tailwindcss/vite` / `eslint-plugin-react-refresh`。
- [x] 追従修正: `NodeJS.Timeout` 型依存の解消、`import.meta.env` を直接書き換えるテストの撤去。

### 実行コマンド

- `./scripts/test-docker.ps1 build`
- `./scripts/test-docker.ps1 all -NoBuild`
- `gh act --bind --workflows .github/workflows/test.yml --job format-check --container-options "--user 0" -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:full-latest --env NPM_CONFIG_PREFIX=/tmp/npm-global`（ログ: `tmp/logs/gh_act_format-check_20251218-153152.log`）
- `gh act --bind --quiet --workflows .github/workflows/test.yml --job native-test-linux --container-options "--user 0" -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:full-latest --env NPM_CONFIG_PREFIX=/tmp/npm-global --env CARGO_TEST_THREADS=1 --env RUST_TEST_THREADS=1`（ログ: `tmp/logs/gh_act_native-test-linux_20251218-181850.log`）

## kukuri-tauri 依存関係更新（@types/node/typescript/jsdom）

- [x] `@types/node` を `^25.0.3` へ更新。
- [x] `typescript` を `~5.9.3` へ更新。
- [x] `jsdom` を `^27.3.0` へ更新。
- [x] 追従: `@typescript-eslint/eslint-plugin` / `@typescript-eslint/parser` を `^8.50.0` へ更新（TypeScript 5.9 の peer 対応）。

### 実行コマンド

- `./scripts/test-docker.ps1 ts`
- `./scripts/test-docker.ps1 lint`
- `gh act --bind --workflows .github/workflows/test.yml --job format-check --container-options "--user 0" -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:full-latest --env NPM_CONFIG_PREFIX=/tmp/npm-global --artifact-server-path tmp/act-artifacts`（ログ: `tmp/logs/gh_act_format-check_20251218-221158.log`）
- `gh act --bind --workflows .github/workflows/test.yml --job native-test-linux --container-options "--user 0" -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:full-latest --env NPM_CONFIG_PREFIX=/tmp/npm-global --env CARGO_TEST_THREADS=1 --env RUST_TEST_THREADS=1 --artifact-server-path tmp/act-artifacts`（ログ: `tmp/logs/gh_act_native-test-linux_20251218-221728.log`）
