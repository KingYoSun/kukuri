# 2026?01?24? ?????

?????: 2026?01?24?

## Community Nodes M1 ???/CI??

- [x] cn-core ? config/metrics/http ????????????????????????
- [x] cn-cli ? clap ????????????????????????????
- [x] CI ? community-node-tests ????????Rust 1.88 ? community node ? cargo test ????????????

### ??

- `docker run --rm -v "<workspace>:/workspace" -w /workspace/kukuri-community-node rust:1.88-bookworm cargo test --workspace --all-features`

## Community Nodes M2 (bootstrap/relay 統合) 完了

- [x] relay/bootstrap/user/admin API の M2 実装範囲を完了
- [x] admin console 最小画面（Services/Subscriptions/Policies/Audit & Health）を実装
- [x] docker-compose/.env を更新し Node Key/Personal Data などの運用設定を反映

## Community Nodes M3 (Index v1) 完了

- [x] cn-index/outbox consumer + Meilisearch 同期（reindex/expiration sweep 含む）を実装
- [x] User API に search/trending + 課金メータリングを追加
- [x] Admin API に reindex ジョブ投入を追加
- [x] docker-compose に index/meilisearch を追加

### 検証

- `docker run --rm -v "<workspace>:/workspace" -w /workspace/kukuri-community-node rust:1.88-bookworm cargo test --workspace --all-features`

## Community Nodes M4 (Moderation v1/v2準備) 完了

- [x] moderation のデータモデル（rules/jobs/labels）と default config を追加
- [x] moderation worker/outbox 追従/label 発行/LLMProvider 抽象を実装
- [x] User API の report 受理 + label 参照、Admin API/Console のルール管理/監査を追加

### 検証

- `docker run --rm -v "<workspace>:/workspace" -w /workspace/kukuri-community-node rust:1.88-bookworm cargo test --workspace --all-features`（警告: `dead_code` など）
- `./scripts/test-docker.ps1 rust`（警告: `dead_code` など）
- `gh act --workflows .github/workflows/test.yml --job format-check`（警告: `git clone` の `some refs were not updated`、`pnpm approve-builds`）
- `gh act --workflows .github/workflows/test.yml --job native-test-linux`（警告: `git clone` の `some refs were not updated`、`pnpm approve-builds`、`useRouter must be used inside a <RouterProvider>`）
