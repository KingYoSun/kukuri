services:
  node_a:
    build:
      context: ..
    env_file:
      - .env.node_a
    command: ["bootstrap"]
    networks:
      - kukuri-cli-test

  node_b:
    build:
      context: ..
    depends_on:
      - node_a
    env_file:
      - .env.node_b
    command: ["connect"]
    networks:
      - kukuri-cli-test

networks:
  kukuri-cli-test:
    driver: bridge
