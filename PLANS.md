# PLANS.md — 実装計画

人間・AIが、依頼された作業を有限な範囲で完了するための計画指針。
承認、リスク区分、固定AC / INVAR、独立監査、Closeの条件は
[Issue lifecycle runbook](docs/runbooks/issue-lifecycle.md)を正本とする。
既存仕様と今回の変更要求の扱いは[docs/README.md](docs/README.md)に従う。

## 計画が必要な場合

- 複数の責務・段階にまたがる変更、仕様判断、migration、リスク区分B/Cでは、実装前に作業と検証の対応を整理する。
- Issueや既存計画に対象・受入条件・作業・検証が揃っていれば再作成せず、不足分だけ補う。
- 区分Aの誤字・局所文言・機械的保守は、対象、期待結果、確認方法をIssue・PRまたは依頼への回答に短く示せばよい。独立した計画ファイルは不要。
- 計画のみの依頼は計画の提示で終了する。実行まで依頼・承認されている場合は、その範囲内で計画を補って続行する。詳細はrunbookの「承認と作業範囲」に従う。

## 粒度と終了条件

計画には、目的、対象外、完了条件、対象path、作業ごとの受入条件と検証、依存関係を含める。
IssueのAC / INVARを参照し、条件と作業・証跡を対応付ける。B/Cで必要なinventoryと状態遷移はrunbookの形式を使う。
同じ情報をIssue・計画・PRで独立管理せず、詳細の置き場所へリンクする。

- 原則はPhase → Taskの2階層まで。4 Phase・8 Task程度を見通しの目安とし、短い計画はTask表だけでよい。
- 1 Taskは、独立に受入判定できる成果物またはまとまった変更結果とする。ファイルを開く・保存する等の操作へ分解しない。
- 対象、期待結果、検証方法が明確で、追加分解が手順の列挙にしかならない時点で分解を止める。
- 目安を超える場合は重複作業を統合し、なお必要なら理由を一文添えて全必須作業を記載する。件数合わせのために必須要件を対象外へ移さず、重大な未解決事項を隠さない。依頼範囲を変える調整だけユーザーに確認する。
- 不明点は既知の事実、作業仮定、実行を妨げる判断点に分ける。関連する質問はまとめ、回答に依存しない作業は進める。件数による切捨てはしない。
- 将来の仮定だけに基づく一般化、任意のpolish、具体的な問題と結び付かない調査を完了条件へ追加しない。

## 記録形式

次は標準の雛形。内容が同じなら日本語の見出し、箇条書き、既存Issue・PRの欄を利用してよい。
非該当欄は省略できる。必要な条件・検証の未実施・未決事項は省略しない。

```md
## 目的・対象外

## 前提・完了条件
- リスク区分 / Scope revision / 基準commit:
- AC / INVARと根拠への参照:
- B/C: inventory / transitionへの参照（Cはsensitive sinkと独立監査も）:

## 作業
| ID | AC / INVAR | 作業・対象path | 受入条件 | 検証・証跡 | 依存 |
| --- | --- | --- | --- | --- | --- |
| T1 | AC-1 / INVAR-1 | ... | ... | ... | なし |

## 未決事項

## 次の一手
```

調査Taskでは対象と決めたいことを明示し、根拠付きの判断記録を成果物とする。
固定対象の判断が終われば調査を終了し、承認済みの範囲で実装へ進む。
時間枠で調査を終了する場合も、残る必須条件を未確認として記録する。

## 作業例

区分Aのrunbookのコマンド名誤記なら、「対象の記述を現行の登録名に合わせ、登録箇所・関連参照と
`git diff --check`を確認する」で足りる。製品testを追加する必要はない。

認証境界の不具合なら区分Cとして、runbookの固定inventory、状態遷移、sensitive sinkの逆引きに基づき、
未認証で禁止I/Oが0回である失敗testと、認証済み経路の回帰testを先に対応付ける。
UIで拒否画面が出たことだけで完了にせず、固定したPR headの独立監査と必須CIを別工程に置く。

## 更新と完了

追加発見はrunbookのExisting-gap / Regression / New-requirement / Optional-hardeningで分類する。
既存条件の不足は該当Taskへ統合し、新要件を黙って足さない。変更のない対象への同じ調査・検証は、
新しい失敗や未解決の懸念がない限り繰り返さない。

全AC / INVARの証跡と必要な検証が揃えば作業を終了する。未実行・失敗・中断は成功と区別し、
IssueのComplete判定はrunbookのマージ後確認に従う。
計画指針自体の見直しは[docs/README.md](docs/README.md)の規則見直し方針に従い、
計画の膨張だけでなく停滞や負担も評価する。行数や禁止事項の増減自体を成果にしない。
