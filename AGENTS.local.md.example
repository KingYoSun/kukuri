# AGENTS.local.md（個人ローカル設定の雛形）

## 言語

- 思考・回答は日本語で行う。

## コードベース調査

- コードの理解・探索は grep / find / ファイル読みより codegraph を優先する。
  - MCP: `codegraph_explore` / `codegraph_node`
  - Shell: `codegraph explore "<質問や symbol 名>"` / `codegraph node <symbol-or-file>`
- `.codegraph/` が無いリポジトリでは codegraph を使わない。
